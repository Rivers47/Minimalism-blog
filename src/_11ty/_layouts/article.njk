---
title: Articolo del Blog
description: Questa è la descrizione di uno degli articoli del blog
tags: post
date: Created
location: 
type: article
---
<!DOCTYPE html>
<html lang="en">
{% include "head-article.njk" %}
<!-- Start Body -->
<body>
    {% include "nav.njk" %}
    <article>
        {% if lang %}
            <small class="info">
                {{ location }}&nbsp;
                <time datetime="{{page.date}}">{{page.date | readableDate}}</time>&nbsp;&ndash;&nbsp;
                {{content | readingTime}}
            </small>
            <h1 style="font-family: Zen Kaku Gothic New;">{{ title }}</h1>
            <div style="font-family: Zen Kaku Gothic New;">
                {{content | safe}}
            </div>
        {% else %}
            <small class="info">
                {{ location }}&nbsp;
                <time datetime="{{page.date}}">{{page.date | readableDate}}</time>&nbsp;&ndash;&nbsp;
                {{content | readingTime}}
            </small>
            <h1>{{ title }}</h1>
            {{content | safe}}
        {% endif %}
        
    </article>
    {% include "footer.njk" %}
</body>
<!-- End Body -->
</html>
